[package]
name = "lib"
version = "0.1.0"
edition = "2021"

[dependencies]
heapless = "0.8.0"
embedded-can = "0.4.1"
statics = { path = "../statics" }
arraydeque = { version = "0.5.1", default-features = false }

[features]
std = []
default = ["std"]
